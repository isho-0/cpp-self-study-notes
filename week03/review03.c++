#include <iostream>

using namespace std;

int main() {
    int a, b, c, d;

    cin >> a >> b >> c >> d;

    if (a == 1, b == 2, c == 3, d == 4) {
        cout << "로그인 성공" << endl;
    } else {
        cout << "로그인 실패" << endl;
    }

    return 0;
}
